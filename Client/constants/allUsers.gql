import gql from 'graphql-tag'

export const ALL_USERS = gql`query allUsers{
    allUsers(orderBy: name_ASC) {
        id,
        email,
        name,
        company,
        isAdmin,
        events {
            id
        },
        meetings {
            id
        }
    }
}`;